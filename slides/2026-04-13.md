---
title: Unified Post-Training
author: Bo Xu
summary:
tags:
  - LLM
  - RL
  - SFT
published: true
created: 2026-04-13 09:38:36
modified: 2026-04-13 09:38:36
---
# Unified Post-Training
打破 SFT 和 RL 的两阶段割裂，实现单阶段/统一后训练
如下我把 **Unified Post-Training** 拆成四个层次：沿着 **目标/梯度、数据、调度、优化层级** 这四个层次分类目前的工作

## 目标函数与梯度估计的统一
粗糙来说其核心思想：SFT 和 RL 表面上一个是监督学习、一个是强化学习，但从策略优化角度看，它们都在更新同一个语言策略，只是采用了不同的数据分布、不同的 advantage 构造方式、不同的稳定化手段。因此，“统一”首先可以发生在 **梯度估计器** 或 **目标函数分解** 上

### Towards a Unified View of Large Language Model Post-Training (HPT / UPGE)

它推导出一个 **Unified Policy Gradient Estimator, UPGE**，把大范围的后训练方法都写成同一目标下的不同梯度估计形式，并拆成四个可替换组件：**stabilization mask、reference policy denominator、advantage estimate、likelihood gradient**。它的关键贡献不是单独发明一个新 loss，而是说明很多后训练算法其实共享一套更底层的梯度骨架。基于这个统一视角，作者又提出 **HPT**，在训练中动态切换不同训练信号

### ORPO: Monolithic Preference Optimization without Reference Model

ORPO 可以看作这一层里更“工程化”的统一方式。它不再把“先 SFT、再 preference optimization”拆成两个阶段，而是直接将偏好对齐（Alignment）直接融入 SFT 阶段。它不需要参考模型（Reference Model），也不需要单独的 RL 阶段，而是直接在传统的 SFT 交叉熵 Loss 后面加了一个基于赔率比（Odds Ratio）的惩罚项，但它清楚展示了：**alignment 目标本身可以被写进一个单阶段、单目标的训练式中**

## 数据来源与策略分布的统一
核心思想：把问题从“目标不同”转成“数据不同”。SFT 主要利用 **offline demonstrations**，RL 主要利用 **on-policy rollouts**。很多新工作开始认为，真正的统一不是把两个 loss 硬加起来，而是把 **off-policy 数据** 和 **on-policy 数据** 当成同一个策略改进问题中的两类轨迹来源。这样一来，SFT 和 RL 的区别就从“方法学标签”变成了“轨迹来源与 credit assignment 的不同”

### Large Language Model Post-Training: A Unified View of Off-Policy and On-Policy Learning
这篇 2026 年的综述提出一个非常清楚的顶层组织方式：后训练首先按 **trajectory provenance** 来分，即 **off-policy learning** 用外部提供的轨迹，**on-policy learning** 用模型自己生成的 rollout。这个框架的好处是，它不再按“SFT、DPO、RL、distillation”这种传统标签来讲，而是直接抓住统一后训练真正的主轴：**模型是在向谁学习，以及这些轨迹是怎么来的**

### CHORD: On-Policy RL Meets Off-Policy Experts
CHORD 明确用 **off-policy vs. on-policy** 的视角来重写 SFT 与 RL 的关系，把 SFT 不再看成一个独立阶段，而是看成 on-policy RL 过程中的 **动态加权辅助目标**。它同时设计了全局系数和 token-level 权重，让模型既能利用专家数据，又不至于被离线数据过度牵着走。这个视角非常重要，因为它说明：统一训练的核心不是“都叫 post-training”，而是 **不同来源轨迹如何共同塑造策略**

### HPT
HPT 其实也属于这一层，因为它从一开始就把后训练数据分成 **online rollouts** 和 **offline demonstrations** 两类，并把它们放入同一个统一策略梯度框架来分析。也就是说，HPT 同时横跨“梯度统一”和“数据统一”两层